/**
 * Server removed for client-only deployment.
 * If you later want to add a custom API server, replace this file with your server code.
 */

console.info("Server entrypoint is disabled. Run `npm run dev` to start the Vite client.");
export {};
